import React from 'react';

const Login = ({url}) => {
    return(
        <div className="loginWrapper">
            <a href={url}>Login</a>
        </div>
        
    );
    
}
export default Login;